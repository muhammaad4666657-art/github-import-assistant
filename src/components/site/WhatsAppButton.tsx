// Re-export SupportChat as the floating widget. WhatsApp launcher removed
// from sitewide chrome; product page still has its own "Order on WhatsApp" CTA.
export { SupportChat as WhatsAppButton } from "./SupportChat";
